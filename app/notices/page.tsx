import {createClient} from '@supabase/supabase-js';
export const dynamic='force-dynamic';
export default async function Notices(){const sb=createClient(process.env.NEXT_PUBLIC_SUPABASE_URL!,process.env.NEXT_PUBLIC_SUPABASE_ANON_KEY!);const {data}=await sb.from('notices').select('*').eq('published',true).order('published_at',{ascending:false});return <main className="container"><h1>Notice Board</h1><p className="muted">Latest school announcements.</p><div className="grid">{(data||[]).map(n=><article className="card notice" key={n.id}><h2>{n.title}</h2><p>{n.body}</p><small className="muted">{new Date(n.published_at).toLocaleDateString('en-IN')}</small>{n.attachment_url&&<p><a className="btn dark" href={n.attachment_url} target="_blank">Open attachment</a></p>}</article>)}</div>{!data?.length&&<div className="card"><p>No notices published yet.</p></div>}</main>}
